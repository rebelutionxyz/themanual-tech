/**
 * ACCOUNT hub accent — the personal-home warm honey. Kept in its own tiny module
 * (mirrors BAZAAR_ACCENT) so both CommunityLayout's ACCENT map and the hub's own
 * tabs read one source without eager-importing the lazy page bundle.
 */
export const ACCOUNT_ACCENT = '#B45309';
