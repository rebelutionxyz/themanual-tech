{
  "name": "fund",
  "version": "0.1.0",
  "private": true,
  "description": "FUND — the crowdfunding astra (platform surface slug `fund`). Served behind a path mount on the manual; the assigned domain is not attached. No host is named in this tree.",
  "scripts": {
    "dev": "next dev",
    "prebuild": "node scripts/build-guard.mjs && node scripts/href-check.mjs && node scripts/env-check.mjs",
    "build": "next build",
    "start": "next start -H ::",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "next": "^16.2.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "@types/react": "19.1.8",
    "@types/react-dom": "19.1.6",
    "typescript": "5.7.2"
  }
}
