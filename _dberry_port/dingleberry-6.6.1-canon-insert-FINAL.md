### §6.6.1 — DingleBERRY Command Center (surface spec + integration) — 2026-06-11

DingleBERRY (P22 · Layer 5 Specialty **security Astra** · the platform immune system; "DINGLEBERRY receives. Always.") — role = malware/threat monitor + **class-action launcher** — gets its first concrete surface: the **Command Center**, ported from a Claude-Design artifact into `TheMANUAL.tech` as slug `dingleberry`, mirroring the INTEL surface pattern.

**Classification (locked Jun-11):** DingleBERRY is the **security** Astra — not a "Justice" anything. Three distinct "Justice" senses, kept separate: (1) the **legal Astra = AtlasADVOCATE.com** (existing — there is NO separate "Justice" Astra); (2) **Justice = TheMANUAL atom realm #1** (knowledge taxonomy, orthogonal to Astra classification); (3) **JUSTICE = the design-system lineage** of this artifact's typography. Rebelution.app is reserved/unassigned — NOT the legal hub. Any prior note assigning DingleBERRY to a "Justice realm" is void.

**Design language (lineage = JUSTICE design system):** Source Serif 4 (authority) / Public Sans (UI) / IBM Plex Mono (record). Palette navy #1B3A5B + gold #B8902F. 181 design tokens (`tokens.css`).

**Screen inventory:** Shell (Sidebar/TopBar/RightRail/PostureBanner/GoDark/DBSeal) + S01 InfraHealth · S02 TransactionSecurity · S03 SourceVerification · ThreatInterception · ShillDetection · DispatchAuth · KarmaCredit · MemberMesh · AtlasOracle · JusticeHandoff.

**JusticeHandoff = the class-action launcher (locked):** DingleBERRY packages the evidence; the affected members form a **Manual Group in TheMANUAL's Justice _realm_** (the class-action record — realm, not Astra). The **legal Astra (AtlasADVOCATE)** is the representation/action layer the class action engages. DingleBERRY never runs the group — it launches and hands off.

**Integration method (locked):** contract-first port. Screens re-authored as repo components against a typed data seam (`useDingleberryData()` / `contract.ts`); render on mock, then swap mock→live per screen. Executor = Claude Code (UR lane), NOT Design. Git via Butch.

**Wire order (locked):** S02 TransactionSecurity first — UI over the drain-model + conservation invariant (bling_system_state, bling_transactions, economy_integrity_check(), economy_integrity_log). Then S03 (CoV) + ShillDetection (bee_affiliate_chain + anti-gaming). Mesh screens (S01 InfraHealth, MemberMesh) stay mock until Phase-2 device-sharing builds.

**Safety rule (locked):** READS BEFORE ACTIONS. DingleBERRY enforcement actions (reverse-ledger, quarantine, mute-across-Astra, grant-rank) are the P1 actor-as-parameter risk surface; they wire LAST, post pre-launch security audit, behind the auth.uid()/service-role pattern.

**Out of scope / parked:** the JX "Justice Docket" mobile app + marketing landing bundled in the same artifact (`d3585a6f`) — re-homed as the **AtlasADVOCATE (Legal Astra) design direction**, a separate build item, not the Command Center.
