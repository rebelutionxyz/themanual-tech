-- PROPOSAL_APPLY1 pairing: REBELUTION.events 0001_events_depth_ticketing (applied 2026-08-21 via execute_sql). Additive: tables event_ticket_tiers, event_tickets + indexes + RLS (public tier read; holder-only ticket read; issuance via SECURITY DEFINER RPC) + fn events_claim_ticket mounting depth_charge_bling + depth_rail_entries (E1). FK to events(id). Full byte-identical DDL: REBELUTION.events/db/proposals/0001_events_depth_ticketing.sql
